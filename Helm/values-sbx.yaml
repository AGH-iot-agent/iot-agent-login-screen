# Default values for a "deployment"

# REQUIRED for you to set a value here, name of your service
name: "iot-agent-login-screen"

# Number of pods in deployment, default of not highly available (override on HA environments)
replicaCount: 1

# minReadySeconds
minReadySeconds: false

# Rollback limit
revisionHistoryLimit: 10

priorityClassName: ""

shareProcessNamespace: false

# Image repository location (override if needed)
image:
  tag: ""
  repository: ""
  command: []
  args: []
  imagePullPolicy: IfNotPresent
  imagePullSecrets: []

# DEPRECATED / DO NOT USE, use podSecurityContext instead, this will be removed in a future release
security:
  runAsNonRoot: false
  runAsUser: 1000
  fsGroup: 1000

# Istio VirtualService definitions
istio:
  gateway:
    createIfMissing: false
    namespace: istio-system
    name: istio-ingressgateway
    selector:
      istio: ingressgateway
    port: 80
    protocol: HTTP
  virtualService:
    enabled: true
    name: "iot-agent-login-screen"
    annotations: {}
    hosts:
      - iot-agent-login-screen-sbx.iotag-sbx.com
    gateways:
      - istio-system/istio-ingressgateway
    http:
      - match:
          - uri:
              prefix: /
        route:
          - destination:
              host: iot-agent-login-screen.iotag-sbx.svc.cluster.local
              port:
                number: 80
    # http: []

# Ingress definitions
ingress:
  enabled: false
  name: ""
  nginx_affinity: true
  nginx_class: "traefik"
  ingressClassName: "traefik"
  pathType: ImplementationSpecific
  annotations: {}
  hosts:
    - host: iot-agent-login-screen-sbx.iotag-sbx
      paths:
        - /
  tls: []

# Secondary ingress definition
ingress_secondary:
  enabled: false
  name: ""
  nginx_affinity: true
  nginx_class: "traefik"
  ingressClassName: "traefik"
  pathType: ImplementationSpecific
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - /
  tls: []

# Service definitions
service:
  enabled: true
  annotations: {}
  type: ClusterIP
  port: 80
  targetPort: 80
  name: default-service
  additionalPorts: []

additionalPorts: []

podAnnotations: {}

usingNewRecommendedLabels: false

labels: {}

labelsEnableDefault: true

lifecycle: {}

globalEnvs: []

globalEnvsSecondary: []

extraEnvs: []

tty: false

envFrom: []

usingMemoryKibiBytesEnvs: false

# livenessProbes are used to determine when to restart a container
livenessProbe:
  enabled: true
  initialDelaySeconds: 120
  periodSeconds: 10
  timeoutSeconds: 9
  successThreshold: 1
  failureThreshold: 3
  probeType: httpGet
  scheme: HTTP
  path: /
  port: default-service
  command:
    - ls -la /

# readinessProbes are used to determine when a container is ready to start accepting traffic
readinessProbe:
  enabled: true
  initialDelaySeconds: 20
  periodSeconds: 5
  timeoutSeconds: 4
  successThreshold: 2
  failureThreshold: 2
  probeType: httpGet
  scheme: HTTP
  path: /
  port: default-service
  command:
    - ls -la /

dnsConfig:
  enabled: false
  ndots: 6
  options: []
  searches: []
  nameservers: []

resources:
  limits:
    cpu: 500m
    memory: 32Mi
  requests:
    cpu: 100m
    memory: 32Mi

nodeSelector: {}

tolerations: []

initContainers: []

extraContainers: []
